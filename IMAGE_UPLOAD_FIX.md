# 🔧 Исправление проблемы с загрузкой изображений

## Проблема

После деплоя на VPS изображения из админки не обновляются.

## Причины

1. **Nginx не проксирует `/uploads`** - запросы к загруженным изображениям не доходят до бэкенда
2. **Жестко прописанный localhost** - в админке используется `http://localhost:3001`, который не работает в продакшене

## Решение

### 1. Обновлена конфигурация nginx

Добавлен маршрут для `/uploads` в `nginx.conf` и `nginx-https.conf`:

```nginx
# Проксирование загруженных изображений к бэкенду
location /uploads {
    proxy_pass http://backend:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # Кеширование изображений
    expires 30d;
    add_header Cache-Control "public, immutable";
}
```

### 2. Исправлен код загрузки в админке

В `src/components/admin/AdminMenu.jsx` функция `uploadImage` теперь:
- Определяет правильный базовый URL динамически
- Использует относительные пути в продакшене
- Использует полный URL только в режиме разработки

## Что нужно сделать на сервере

### Шаг 1: Обновите код

```bash
cd /path/to/tomyangbar
git pull
```

### Шаг 2: Пересоберите и перезапустите контейнеры

```bash
docker-compose build
docker-compose up -d
```

### Шаг 3: Проверьте логи (если что-то не работает)

```bash
# Логи frontend (nginx)
docker logs tomyangbar-frontend

# Логи backend
docker logs tomyangbar-backend
```

### Шаг 4: Проверьте права доступа к папке uploads

```bash
# Убедитесь, что папка существует и имеет правильные права
ls -la backend/uploads/

# Если нужно, создайте папку и установите права
mkdir -p backend/uploads
chmod 755 backend/uploads
```

## Проверка работы

1. Зайдите в админку
2. Загрузите новое изображение для блюда
3. Проверьте, что изображение отображается:
   - В админке при редактировании
   - На сайте в меню

## Дополнительные проверки

### Проверка доступности изображений

```bash
# Проверьте, что изображения доступны через nginx
curl http://your-domain.com/uploads/dish-1234567890.jpg

# Должен вернуться файл изображения
```

### Проверка volume монтирования

```bash
# Проверьте, что volume правильно смонтирован
docker exec tomyangbar-backend ls -la /app/uploads

# Должны быть видны загруженные файлы
```

### Проверка прав доступа в контейнере

```bash
# Проверьте права на запись
docker exec tomyangbar-backend touch /app/uploads/test.txt
docker exec tomyangbar-backend rm /app/uploads/test.txt

# Если ошибка, нужно исправить права:
sudo chown -R 1000:1000 backend/uploads
```

## Если проблема остается

1. **Очистите кеш браузера** - возможно, старые изображения закешированы
2. **Проверьте консоль браузера** (F12) - могут быть ошибки загрузки
3. **Проверьте Network вкладку** - куда идут запросы к изображениям
4. **Проверьте логи бэкенда** - могут быть ошибки при загрузке файлов

## Важные замечания

- Изображения сохраняются в `backend/uploads/` на хосте
- Volume монтируется как `./backend/uploads:/app/uploads`
- После пересборки контейнеров изображения сохраняются (благодаря volume)
- Nginx проксирует запросы `/uploads` на бэкенд, который отдает статические файлы

