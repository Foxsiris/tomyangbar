# 🔧 Исправление проблемы с загрузкой изображений в админке на проде

## Проблема
На проде не получается сменить картинку блюда через админку.

## Что было исправлено

### 1. Улучшена функция `uploadImage` в `AdminMenu.jsx`

- Добавлено подробное логирование для отладки
- Улучшена обработка ошибок
- Правильное определение baseURL для продакшена и разработки
- Проверка наличия `imageUrl` в ответе сервера

### 2. Проверка конфигурации

- ✅ Nginx правильно проксирует `/api/admin/upload-image` к бэкенду
- ✅ Nginx правильно проксирует `/uploads/*` к бэкенду
- ✅ Бэкенд сохраняет файлы в `/app/uploads`
- ✅ Бэкенд возвращает относительный путь `/uploads/filename.jpg`

## Диагностика на VPS

### Шаг 1: Проверьте логи при загрузке

```bash
# Логи frontend (nginx)
docker logs tomyangbar-frontend -f

# Логи backend
docker logs tomyangbar-backend -f
```

### Шаг 2: Проверьте доступность эндпоинта

```bash
# Проверьте, что эндпоинт доступен
curl -X POST http://localhost/api/admin/upload-image \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "image=@test.jpg"
```

### Шаг 3: Проверьте права на директорию uploads

```bash
# Проверьте, что директория существует и доступна для записи
docker exec tomyangbar-backend ls -la /app/uploads

# Должны быть права на запись (drwxr-xr-x или drwxrwxrwx)
```

### Шаг 4: Проверьте консоль браузера

1. Откройте DevTools (F12)
2. Вкладка **Console**
3. Попробуйте загрузить изображение
4. Проверьте логи:
   - `Uploading image to: ...` - должен показать правильный URL
   - `Upload response: ...` - должен показать ответ сервера
   - Если есть ошибки - скопируйте их

### Шаг 5: Проверьте Network в DevTools

1. Откройте DevTools (F12) → **Network**
2. Попробуйте загрузить изображение
3. Найдите запрос `upload-image`
4. Проверьте:
   - **Status**: должен быть 200 OK
   - **Request URL**: должен быть `/api/admin/upload-image` (без localhost в продакшене)
   - **Response**: должен содержать `{ imageUrl: "/uploads/..." }`

## Частые проблемы

### Проблема 1: "Failed to fetch" или CORS error

**Причина**: Неправильный baseURL или проблема с CORS

**Решение**:
1. Проверьте, что в продакшене `baseURL = ''` (пустая строка)
2. Проверьте CORS настройки в `backend/src/server.js`
3. Убедитесь, что `FRONTEND_URL` правильно установлен в `.env`

### Проблема 2: "401 Unauthorized"

**Причина**: Токен авторизации не передается или истек

**Решение**:
1. Проверьте, что токен есть в localStorage:
   ```javascript
   localStorage.getItem('tomyangbar_token')
   ```
2. Перелогиньтесь в админку
3. Проверьте, что токен передается в заголовке `Authorization`

### Проблема 3: "400 Bad Request" или "Файл изображения не предоставлен"

**Причина**: FormData не формируется правильно

**Решение**:
1. Проверьте, что файл действительно выбран
2. Проверьте консоль браузера на наличие ошибок
3. Убедитесь, что файл не слишком большой (лимит 5MB)

### Проблема 4: Изображение загружается, но не отображается

**Причина**: Неправильный путь к изображению в базе данных

**Решение**:
1. Проверьте, что в базе данных путь начинается с `/uploads/`
2. Проверьте, что nginx правильно проксирует `/uploads/*`
3. Проверьте, что файл действительно существует:
   ```bash
   docker exec tomyangbar-backend ls -la /app/uploads/
   ```

### Проблема 5: "413 Request Entity Too Large"

**Причина**: Файл слишком большой

**Решение**:
1. Уменьшите размер изображения (максимум 5MB)
2. Или увеличьте лимит в `backend/src/routes/adminRoutes.js`:
   ```javascript
   limits: {
     fileSize: 10 * 1024 * 1024 // 10MB
   }
   ```

## Проверка после исправления

1. Обновите код на VPS:
   ```bash
   git pull
   docker-compose build frontend
   docker-compose up -d
   ```

2. Откройте админку в браузере

3. Попробуйте загрузить изображение для блюда

4. Проверьте:
   - ✅ Изображение загружается без ошибок
   - ✅ Изображение отображается в превью
   - ✅ После сохранения изображение видно в списке блюд
   - ✅ Изображение доступно по URL `/uploads/filename.jpg`

## Дополнительная отладка

Если проблема осталась, включите подробное логирование:

1. В консоли браузера должны быть логи:
   - `Uploading image to: ...`
   - `Upload response: ...`

2. В логах бэкенда должны быть:
   - `Upload image error:` (если есть ошибка)
   - Или успешная загрузка

3. Проверьте ответ сервера в Network tab:
   - Должен быть статус 200
   - Response должен содержать `imageUrl`

## Важно

После изменений **обязательно** пересоберите frontend:

```bash
docker-compose build frontend
docker-compose up -d frontend
```

Просто `docker-compose up -d` может не пересобрать образ!

