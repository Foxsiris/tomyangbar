# 🚀 Быстрое исправление "Failed to fetch"

## Проблема
После деплоя на VPS не грузится меню и заказы, ошибка "Failed to fetch" в консоли.

## Решение (выполните на VPS)

```bash
# 1. Перейдите в папку проекта
cd /path/to/tomyangbar

# 2. Обновите код
git pull

# 3. Пересоберите frontend (ВАЖНО!)
docker-compose build frontend

# 4. Перезапустите все контейнеры
docker-compose up -d

# 5. Проверьте логи
docker logs tomyangbar-backend | tail -20
docker logs tomyangbar-frontend | tail -20
```

## Проверка

1. Откройте сайт в браузере
2. Откройте DevTools (F12) → Network
3. Проверьте запросы к `/api/menu/full` - должны быть успешными (200)

## Если не помогло

Смотрите подробную инструкцию в `PRODUCTION_API_FIX.md`

