# 🔍 Отладка проблемы с меню

## Что было исправлено:

1. ✅ Исправлен формат ответа API - теперь возвращается плоская структура
2. ✅ Добавлено детальное логирование на всех уровнях
3. ✅ Улучшена обработка ошибок и пустых данных
4. ✅ Исправлена фильтрация категорий по `is_active`

## Как проверить, что происходит:

### 1. Откройте консоль браузера (F12)

Вы должны увидеть логи:
- `MenuService.getFullMenu: Starting request...`
- `apiClient.executeRequest: Making request to...`
- `useMenu.loadMenu: Data received...`
- `Menu.filteredDishes: menuData...`

### 2. Проверьте логи бэкенда

В терминале, где запущен бэкенд, вы должны увидеть:
- `GET /menu/full: Request received`
- `GET /menu/full: Fetching categories...`
- `GET /menu/full: Categories result: ...`
- `GET /menu/full: Dishes result: ...`

### 3. Проверьте, что данные есть в базе

Убедитесь, что в Supabase есть:
- Таблица `categories` с записями, где `is_active = true`
- Таблица `dishes` с записями, где `is_active = true`

### 4. Проверьте API напрямую

Откройте в браузере или используйте curl:
```bash
curl http://localhost:3001/api/menu/full
```

Или в браузере:
```
http://localhost:3001/api/menu/full
```

Должен вернуться JSON с полями `menu.categories` и `menu.dishes`.

## Возможные проблемы:

### Проблема: "Меню не получено от сервера"
- Проверьте, что бэкенд запущен
- Проверьте, что URL правильный (в продакшене `/api`, в разработке `http://localhost:3001/api`)
- Проверьте CORS настройки

### Проблема: Пустые массивы categories и dishes
- Проверьте, что в базе данных есть записи
- Проверьте, что `is_active = true` для категорий и блюд
- Проверьте логи бэкенда - возможно есть ошибка Supabase

### Проблема: Ошибка 404 или CORS
- Проверьте, что роут `/api/menu/full` существует
- Проверьте настройки CORS в `backend/src/server.js`

## Следующие шаги:

1. Откройте консоль браузера и посмотрите логи
2. Проверьте логи бэкенда
3. Проверьте API напрямую через браузер/curl
4. Сообщите, что именно вы видите в логах





