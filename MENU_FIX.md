# 🔧 Исправление проблемы с пустым меню

## Проблема
- На проде меню пустое, хотя блюда видны в админке
- Локально ошибка на странице меню

## Причина
Несоответствие формата данных между бэкендом и фронтендом:
- **Бэкенд возвращает**: `{ menu: { categories: [...], dishes: [...] } }`
- **Фронтенд ожидал**: массив или другой формат

## Исправление

### Что было исправлено:

1. **`src/services/menuService.js`** - правильно извлекает `menu` из ответа API
2. **`src/hooks/useMenu.js`** - правильно обрабатывает объект `{ categories: [...], dishes: [...] }`
3. Убраны лишние console.log в продакшене (оставлены только в режиме разработки)

## Что нужно сделать на VPS

```bash
# 1. Обновите код
git pull

# 2. Пересоберите frontend
docker-compose build frontend

# 3. Перезапустите контейнеры
docker-compose up -d

# 4. Проверьте логи (должны быть без ошибок)
docker logs tomyangbar-backend | tail -30
docker logs tomyangbar-frontend | tail -30
```

## Проверка

1. Откройте сайт в браузере
2. Перейдите на страницу меню
3. Меню должно загрузиться с категориями и блюдами
4. Откройте DevTools (F12) → Console - не должно быть ошибок
5. Откройте DevTools → Network → проверьте запрос `/api/menu/full`:
   - Статус: 200 OK
   - Response должен содержать `{ menu: { categories: [...], dishes: [...] } }`

## Локальная проверка

```bash
# Запустите локально
npm run dev

# Откройте http://localhost:5173
# Перейдите на страницу меню
# Проверьте консоль браузера - не должно быть ошибок
```

## Если проблема осталась

1. Проверьте формат данных в ответе API:
   ```bash
   curl http://your-domain.com/api/menu/full
   ```
   
   Должен вернуть:
   ```json
   {
     "menu": {
       "categories": [...],
       "dishes": [...]
     }
   }
   ```

2. Проверьте логи бэкенда:
   ```bash
   docker logs tomyangbar-backend | grep "GET /menu/full"
   ```

3. Проверьте консоль браузера (F12) на наличие ошибок

