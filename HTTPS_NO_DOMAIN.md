# 🔒 HTTPS без домена (самоподписанный сертификат)

Если у вас нет домена, но вы хотите использовать HTTPS, можно использовать самоподписанный SSL сертификат.

## ⚠️ Важные ограничения

1. **Предупреждение в браузере**: Браузеры будут показывать предупреждение о безопасности при первом посещении
2. **Не для продакшена**: Не подходит для реальных пользователей, только для тестирования или внутреннего использования
3. **Нет автоматического обновления**: Сертификат нужно обновлять вручную

## 🚀 Быстрая настройка

### Автоматически (рекомендуется)

```bash
# На сервере, в папке проекта
# Замените YOUR_IP на IP вашего сервера (или оставьте localhost)
./setup-https-no-domain.sh YOUR_IP
```

Пример:
```bash
./setup-https-no-domain.sh 192.168.1.100
# или для localhost
./setup-https-no-domain.sh localhost
```

### Вручную

#### 1. Остановите контейнеры
```bash
docker-compose down
```

#### 2. Сгенерируйте самоподписанный сертификат
```bash
# Замените YOUR_IP на IP вашего сервера
./generate-self-signed-cert.sh YOUR_IP
```

#### 3. Обновите .env файл
```bash
nano .env
```

Измените:
```env
FRONTEND_URL=https://YOUR_IP
```

#### 4. Переключитесь на HTTPS конфигурацию
```bash
cp docker-compose.http.yml docker-compose.yml 2>/dev/null || true
cp docker-compose.https.yml docker-compose.yml
```

#### 5. Запустите проект
```bash
docker-compose up -d --build
```

## 📋 Использование

### Доступ к сайту

Откройте в браузере: `https://YOUR_IP`

### Предупреждение в браузере

При первом посещении браузер покажет предупреждение:

**Chrome/Edge:**
```
Ваше подключение не защищено
NET::ERR_CERT_AUTHORITY_INVALID
```
→ Нажмите "Дополнительно" → "Перейти на сайт (небезопасно)"

**Firefox:**
```
Предупреждение: возможна угроза безопасности
```
→ Нажмите "Дополнительно" → "Принять риск и продолжить"

**Safari:**
```
Это соединение не является частным
```
→ Нажмите "Показать подробности" → "Посетить этот веб-сайт"

Это нормально для самоподписанных сертификатов!

## 🔄 Обновление сертификата

Самоподписанный сертификат действителен 365 дней. Для обновления:

```bash
# Остановите контейнеры
docker-compose down

# Сгенерируйте новый сертификат
./generate-self-signed-cert.sh YOUR_IP

# Запустите снова
docker-compose up -d
```

## 🆚 Сравнение: самоподписанный vs Let's Encrypt

| Параметр | Самоподписанный | Let's Encrypt |
|----------|----------------|--------------|
| Нужен домен | ❌ Нет | ✅ Да |
| Предупреждение в браузере | ⚠️ Да | ✅ Нет |
| Подходит для продакшена | ❌ Нет | ✅ Да |
| Автообновление | ❌ Нет | ✅ Да |
| Срок действия | 365 дней | 90 дней |

## 💡 Рекомендации

### Для тестирования / разработки
✅ Используйте самоподписанный сертификат

### Для продакшена
✅ Используйте домен с Let's Encrypt (см. [HTTPS_SETUP.md](./HTTPS_SETUP.md))

### Для внутренней сети
✅ Самоподписанный сертификат подойдет, если пользователи готовы игнорировать предупреждение

## 🐛 Решение проблем

### "openssl: command not found"
Установите openssl:
```bash
# Ubuntu/Debian
sudo apt install openssl

# CentOS/RHEL
sudo yum install openssl
```

### "SSL certificate not found"
Проверьте файлы:
```bash
ls -la ssl/
# Должны быть: fullchain.pem и privkey.pem
```

Если файлов нет, сгенерируйте заново:
```bash
./generate-self-signed-cert.sh YOUR_IP
```

### Браузер не принимает сертификат
1. Убедитесь, что используете правильный IP в URL
2. Очистите кеш браузера
3. Попробуйте в режиме инкогнито
4. Проверьте, что сертификат создан для правильного IP

## 📝 Примечания

- Сертификат создается для конкретного IP/домена
- Если IP изменится, нужно создать новый сертификат
- Для нескольких IP можно создать сертификат с несколькими SAN (Subject Alternative Names)

## 🔄 Откат на HTTP

Если нужно вернуться на HTTP:

```bash
docker-compose down
cp docker-compose.http.yml docker-compose.yml
docker-compose up -d
```






