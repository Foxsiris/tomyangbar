# 🔧 Исправление проблемы "Failed to fetch" в продакшене

## Проблема

После деплоя на VPS:
- Не грузится меню
- Не грузятся заказы
- Ошибка "Failed to fetch" в консоли браузера

## Причина

API клиент в продакшене пытается обращаться к `http://localhost:3001`, который недоступен из браузера.

## Решение

### 1. Исправлен apiClient.js

Теперь API клиент правильно определяет режим:
- **Разработка**: `http://localhost:3001` (прямой доступ к бэкенду)
- **Продакшен**: относительные пути `/api` (проксируются через nginx)

### 2. Проверка на сервере

#### Шаг 1: Обновите код

```bash
cd /path/to/tomyangbar
git pull
```

#### Шаг 2: Пересоберите и перезапустите

```bash
docker-compose build
docker-compose up -d
```

#### Шаг 3: Проверьте логи

```bash
# Логи frontend (nginx)
docker logs tomyangbar-frontend

# Логи backend
docker logs tomyangbar-backend
```

#### Шаг 4: Проверьте доступность API

```bash
# Проверьте health check
curl http://localhost/api/health

# Должен вернуться JSON с status: "OK"
```

### 3. Диагностика проблем

#### Проверка 1: Бэкенд запущен?

```bash
docker ps | grep tomyangbar-backend
# Должен быть запущен

# Проверьте логи
docker logs tomyangbar-backend | tail -20
```

#### Проверка 2: Nginx проксирует правильно?

```bash
# Проверьте конфигурацию nginx
docker exec tomyangbar-frontend cat /etc/nginx/conf.d/default.conf | grep -A 10 "location /api"

# Должно быть:
# location /api {
#     proxy_pass http://backend:3001;
#     ...
# }
```

#### Проверка 3: Сеть между контейнерами работает?

```bash
# Проверьте, что frontend может достучаться до backend
docker exec tomyangbar-frontend ping -c 2 backend

# Или проверьте через curl
docker exec tomyangbar-frontend wget -O- http://backend:3001/api/health
```

#### Проверка 4: CORS настройки

Убедитесь, что в `.env` на сервере установлен правильный `FRONTEND_URL`:

```env
FRONTEND_URL=http://your-domain.com
# или
FRONTEND_URL=https://your-domain.com
```

После изменения `.env` перезапустите контейнеры:
```bash
docker-compose down
docker-compose up -d
```

### 4. Проверка в браузере

1. Откройте DevTools (F12)
2. Вкладка **Network**
3. Попробуйте загрузить страницу
4. Проверьте запросы к `/api/*`:
   - Должны идти на ваш домен (не localhost)
   - Статус должен быть 200 (не Failed)
   - Если 502/503/504 - проблема с проксированием

### 5. Частые проблемы

#### "502 Bad Gateway"

**Причина**: Nginx не может достучаться до backend

**Решение**:
```bash
# Проверьте, что backend запущен
docker ps | grep backend

# Проверьте логи backend
docker logs tomyangbar-backend

# Перезапустите backend
docker-compose restart backend
```

#### "CORS error"

**Причина**: Неправильный FRONTEND_URL в .env

**Решение**:
```bash
# Проверьте .env
cat .env | grep FRONTEND_URL

# Установите правильный URL
nano .env
# FRONTEND_URL=http://your-domain.com

# Перезапустите
docker-compose restart backend
```

#### "Failed to fetch" или "Network error"

**Причина**: Запросы идут на localhost:3001 вместо относительных путей

**Решение**:
1. Убедитесь, что код обновлен (`git pull`)
2. Пересоберите frontend:
```bash
docker-compose build frontend
docker-compose up -d frontend
```

### 6. Проверка после исправления

1. Откройте сайт в браузере
2. Откройте DevTools → Network
3. Проверьте запросы:
   - `/api/menu/full` - должен вернуть меню
   - `/api/orders` - должен вернуть заказы (если авторизованы)
4. В консоли не должно быть ошибок "Failed to fetch"

## Важно

После изменений в коде **обязательно** пересоберите frontend:

```bash
docker-compose build frontend
docker-compose up -d frontend
```

Просто `docker-compose up -d` может не пересобрать образ, если код изменился!

